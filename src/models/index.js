const { Renter } = require('./renter');
const { Property } = require('./property');
const { PropertyOwner } = require('./propertyOwner');

module.exports = {
    Renter,
    Property,
    PropertyOwner
};
